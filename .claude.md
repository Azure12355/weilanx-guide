# 蔚蓝空间栈 - VuePress 项目编码规范

> 本文档作为 Claude AI 助手在该项目中的编码规范和重要信息记录

## 项目概述

- **项目名称**: weilanx-guide
- **网站标题**: 蔚蓝空间栈
- **网站地址**: https://guide.weilanx.com
- **作者**: 蔚蓝Lynx (https://www.weilanx.com)
- **GitHub**: azure12355/weilanx-guide
- **项目类型**: 基于 VuePress 2 的技术文档/博客网站

## 技术栈

### 核心框架
- **VuePress**: 2.0.0-rc.26
- **Bundler**: @vuepress/bundler-vite (Vite)
- **Theme**: vuepress-theme-hope 2.0.0-rc.101
- **Node.js**: 20.x
- **包管理器**: pnpm v9

### 开发依赖
- Vue 3.5.26
- sass-embedded 1.97.1
- TypeScript (ES2022, NodeNext)

### 样式处理
- **预处理器**: SCSS
- **配置**: 屏蔽 legacy-js-api, if-function, import 警告
- **quietDeps**: true (忽略依赖包警告)

## 目录结构

```
vuepress/
├── .github/
│   └── workflows/
│       └── deploy-docs.yml      # GitHub Actions 自动部署配置
├── vuepress-hope/               # 主要内容目录 (源码目录)
│   ├── .vuepress/               # VuePress 配置目录
│   │   ├── config.ts            # 站点基础配置
│   │   ├── theme.ts             # 主题配置
│   │   ├── navbar/              # 导航栏配置
│   │   │   └── index.ts         # 导航栏结构定义
│   │   ├── sidebar/             # 侧边栏配置
│   │   │   └── index.ts         # 侧边栏结构定义
│   │   ├── public/              # 静态资源 (图片、favicon等)
│   │   ├── styles/              # 自定义样式 (SCSS)
│   │   ├── .cache/              # 构建缓存 (gitignore)
│   │   ├── .temp/               # 临时文件 (gitignore)
│   │   └── dist/                # 构建输出 (gitignore)
│   ├── index.md                 # 网站首页
│   ├── guide/                   # 通用指南文档
│   ├── java/                    # Java 相关内容
│   │   ├── index.md
│   │   ├── base/                # Java 基础篇
│   │   │   ├── 00-overview/     # Java 概述 & 环境安装
│   │   │   ├── 01-syntax/       # Java 基础语法
│   │   │   ├── 03-array/        # Java 数组
│   │   │   └── appendix/        # 附录 (数学、正则)
│   │   ├── high/                # Java 进阶篇
│   │   ├── juc/                 # JUC 并发编程
│   │   └── jvm/                 # JVM 虚拟机
│   ├── algo/                    # 算法题单 (待添加)
│   └── 408/                     # 计算机基础 (待添加)
├── package.json                 # 项目配置和脚本
├── pnpm-lock.yaml              # 依赖锁定文件
├── tsconfig.json               # TypeScript 配置
├── .gitignore                  # Git 忽略规则
└── .claude.md                  # 本文件

```

## 导航栏结构

当前导航栏配置 (`.vuepress/navbar/index.ts`):

### 1. Java攻城狮 (`/java/`)
- Java基础篇 (`/java/base/`)
- Java进阶篇 (`/java/high/`)
- JUC并发编程 (`/java/juc/`)
- JVM虚拟机 (`/java/jvm/`)

### 2. 剑指offer (`/algo/`)
- LeetCode题单 (`/algo/manual/`)
- LeetCode Hot100 (`/algo/hot/`)
- code top (`/algo/codetop/`)

### 3. 计算机基础 (`/408/`)
- 数据结构 (`/408/ds/`)
- 计算机组成原理 (`/408/co/`)
- 操作系统 (`/408/os/`)
- 计算机网络 (`/408/cn/`)

### 4. VibeCoding (待定)
- 当前为占位内容

## 常用脚本命令

```bash
# 开发服务器 (带缓存)
pnpm dev

# 开发服务器 (清除缓存)
pnpm clean-dev

# 生产构建
pnpm build

# 更新 VuePress 包
pnpm update-package
```

## 配置文件说明

### config.ts (`.vuepress/config.ts`)
- **base**: "/" (根路径)
- **语言**: zh-CN
- **bundler**: Vite + SCSS 预处理
- **SCSS 配置**:
  - silenceDeprecations: ['legacy-js-api', 'if-function', 'import']
  - quietDeps: true

### theme.ts (`.vuepress/theme.ts`)
- **hostname**: https://guide.weilanx.com/
- **favicon**: /logo.svg
- **docsDir**: "src" (注意: 实际源码在 vuepress-hope 目录)
- **TOC**: 显示 1-6 级标题
- **加密**: 支持 (示例密码: 1234)

### Markdown 扩展支持
- ✅ align (对齐)
- ✅ attrs (属性)
- ✅ codeTabs (代码选项卡)
- ✅ component (组件)
- ✅ demo (演示)
- ✅ figure (图表)
- ✅ gfm (GitHub 风格 Markdown)
- ✅ imgLazyload (图片懒加载)
- ✅ imgSize (图片尺寸)
- ✅ include (文件引入)
- ✅ mark (标记)
- ✅ plantuml (PlantUML 图表)
- ✅ spoiler (剧透折叠)
- ✅ stylize (样式自定义)
- ✅ sub/sup (下标/上标)
- ✅ tabs (标签页)
- ✅ tasklist (任务列表)
- ✅ vPre (跳过 Vue 解析)

### 可选功能 (需安装依赖)
- math (katex/mathjax) - 数学公式
- chartjs - 图表
- echarts - 数据可视化
- flowchart - 流程图
- mermaid - 图表
- revealjs - 幻灯片
- pwa - 渐进式 Web 应用

## 组件和插件

### 自定义组件
- **Badge**: 徽章组件 (`<Badge type="tip">`)
- **VPCard**: 卡片组件

### 评论系统
- **Giscus** (当前为测试配置，生产环境需更换)
- 需要更新为用户自己的仓库配置

### 图标系统
- **前缀**: fa6-solid: (Font Awesome Solid)
- 使用的图标集: ri, famicons, streamline-sharp, file-icons, game-icons, uiw, el, hugeicons, lsicon, heroicons, devicon-plain, lineicons 等

## 内容组织规范

### 文档命名规范
- 使用小写字母和连字符: `java-keyword.md`
- 或使用下划线: `java_overview.md` (当前混用，建议统一)
- 建议统一使用: 小写字母 + 连字符

### 目录编号规范
- 使用两位数字编号: `00-overview`, `01-syntax`, `03-array`
- 便于排序和扩展

### Frontmatter 规范
```yaml
---
title: 页面标题
icon: 图标名称
order: 排序序号 (可选)
category: 分类 (可选)
tag: 标签 (可选)
---
```

## Git 忽略规则

当前 `.gitignore`:
```
node_modules/
vuepress-hope/.vuepress/.cache/
vuepress-hope/.vuepress/.temp/
vuepress-hope/.vuepress/dist/
vuepress-reco
test/
```

## 自动部署

### GitHub Actions
- **触发**: 推送到 main 分支 / 手动触发
- **运行环境**: Ubuntu + Node.js 20
- **内存配置**: NODE_OPTIONS=--max-old-space-size=8192
- **部署分支**: gh-pages
- **部署目录**: vuepress-hope/.vuepress/dist

## 开发注意事项

### 1. 文件扩展名
- 配置文件使用 `.ts` / `.js` (TypeScript)
- 内容文件使用 `.md` (Markdown)

### 2. 图标引用
- 导航栏和侧边栏使用 icon 属性
- 图标集名称需完整，如: "ri:java-fill", "fa6-solid:lightbulb"

### 3. 侧边栏配置
- 可使用 `"structure"` 自动生成子目录结构
- 可手动指定子文件列表
- collapsible/expanded 控制折叠状态

### 4. 链接引用
- 内部链接使用绝对路径: `/java/base/01-syntax/java-keyword.html`
- 或使用相对路径: `./java-keyword.md`

### 5. 图片资源
- 放置在 `vuepress-hope/.vuepress/public/` 目录
- 引用时使用 `/` 开头: `![图片](/image.png)`

### 6. SCSS 样式
- 自定义样式放在 `.vuepress/styles/` 目录
- SCSS 变量可用于主题定制

## 待办/改进项

- [ ] 统一文档命名规范 (小写+连字符)
- [ ] 添加 algo/ (算法) 目录内容
- [ ] 添加 408/ (计算机基础) 目录内容
- [ ] 更新 Giscus 评论配置为生产环境
- [ ] 考虑启用 PWA 功能
- [ ] 考虑添加数学公式支持 (katex/mathjax)

## 重要提醒

1. **构建目录**: 源码在 `vuepress-hope/`，构建输出在 `vuepress-hope/.vuepress/dist/`
2. **配置类型**: 使用 `.ts` 文件但实际 import 为 `.js` (注意打包配置)
3. **docsDir 配置**: theme.ts 中配置为 "src"，但实际内容在 vuepress-hope，需注意
4. **图标系统**: 支持多种图标集，使用 Iconify 格式
5. **部署目标**: 部署到 gh-pages 分支，通过 GitHub Pages 托管
